@model Library.Domain.Entities.Comment


    @Html.DisplayFor(modelItem => Model.User.FirstName)

    @Html.DisplayFor(modelItem => Model.User.LastName)

    @Html.DisplayFor(modelItem => Model.Text)

    @Html.DisplayFor(modelItem => Model.CreationDate)
